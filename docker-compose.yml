services:
  redis:
    image: redis

    ports:
      - "6379:6379"

  api:
  
    build: .
    
    depends_on:
      - redis
    
    ports:
      - "8000:8000"
  
  worker:

    build: .

    depends_on:
      - redis

    command: python worker.py